import { Layout } from '../layout/Layout';

export const HomePage = () => {
    return (
        <Layout>
            <section>
                <h1>HomePage</h1>
            </section>
        </Layout>
    );
};
