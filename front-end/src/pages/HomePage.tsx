import { FC } from 'react';
import { Layout } from 'layout/Layout';

export const HomePage: FC = () => {
    return (
        <Layout>
            <section>
                <h1>HomePage</h1>
            </section>
        </Layout>
    );
};
